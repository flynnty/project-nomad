name: Build Sidecar Updater Image

on:
  push:
    branches:
      - main
    paths:
      - 'install/sidecar-updater/**'
      - '.github/workflows/build-sidecar-updater.yml'
  workflow_dispatch:
    inputs:
      version:
        description: 'Semantic version to label the Docker image under (no "v" prefix, e.g. "1.2.3")'
        required: true
        type: string
      tag_latest:
        description: 'Also tag this image as :latest?'
        required: false
        type: boolean
        default: false

jobs:
  build:
    name: Build sidecar-updater image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: install/sidecar-updater
          push: true
          tags: |
            ghcr.io/flynnty/project-nomad-sidecar-updater:latest
            ${{ inputs.version && format('ghcr.io/flynnty/project-nomad-sidecar-updater:{0}', inputs.version) || '' }}
            ${{ inputs.version && format('ghcr.io/flynnty/project-nomad-sidecar-updater:v{0}', inputs.version) || '' }}
